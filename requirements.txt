# Alert Dashboard V2 Dependencies

# Web Framework (FastAPI)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Core async and web (kept for compatibility)
aiohttp>=3.9.0
websockets>=12.0
uvloop>=0.19.0; sys_platform != 'win32'

# XMPP for NWWS-OI Weather Wire connection
slixmpp>=1.8.5

# Date/time handling
python-dateutil>=2.8.2
pytz>=2024.1

# Configuration and environment
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging
structlog>=24.1.0

# Data validation and serialization
orjson>=3.9.0

# HTTP client with retry support
httpx>=0.26.0
tenacity>=8.2.0

# Geospatial (for polygon operations)
shapely>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Development
black>=24.1.0
ruff>=0.1.0
mypy>=1.8.0

# Optional: pyIEM for reference patterns (install separately if needed)
# pip install git+https://github.com/akrherz/pyIEM.git
